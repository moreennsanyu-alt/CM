<UserControl x:Class="ClinicManager.Win.Features.Authentication.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             AutomationProperties.AutomationId="LoginView"
             Width="380"
             Height="440"
             Background="#FFF5F5F7">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Sign in to Clinic Manager" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,24" />
        <TextBlock Grid.Row="1" Text="Username"/>
        <TextBox Grid.Row="1" Margin="0,22,0,12" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.AutomationId="UsernameTextBox" />
        <TextBlock Grid.Row="2" Text="Password" />
        <PasswordBox Grid.Row="2" Margin="0,22,0,12" PasswordChanged="PasswordBox_OnPasswordChanged" AutomationProperties.AutomationId="PasswordTextBox"/>
        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="Firebrick" Margin="0,0,0,12" AutomationProperties.AutomationId="LoginStatusLabel"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button AutomationProperties.AutomationId="CancelButton" Content="Cancel" Command="{Binding CancelCommand}" Margin="0,0,8,0" />
            <Button AutomationProperties.AutomationId="LoginButton" Content="Login" Command="{Binding LoginCommand}" IsDefault="True" />
        </StackPanel>
    </Grid>
</UserControl>
